# Phlix Mobile — Agent Context

Cross-platform React Native 0.85 app (`phlix-mobile`) connecting to a media server (Jellyfin/Emby-compatible). TypeScript strict, Zustand state, React Navigation v6, native players via `ios/LocalPods/PhlixPlayer/` (AVPlayer/Swift) and `android/app/src/main/java/com/phlixmobile/player/` (ExoPlayer/Kotlin).

See `@./DEVELOPER.md` for full architecture/native module deep-dive and `@./README.md` for setup/deployment.

## Commands

```bash
npm install                              # install JS deps
cd ios && pod install && cd ..           # iOS pods (macOS only)
npm start                                # Metro bundler
npm run ios                              # react-native run-ios
npm run android                          # react-native run-android
npm test                                 # jest (preset: react-native)
npm test -- --coverage                   # jest with coverage
npm test -- --testPathPattern="stores"   # filter test path
npm run lint                             # eslint . --ext .js,.jsx,.ts,.tsx
npm run typecheck                        # tsc --noEmit
npm run build:android:debug              # ./gradlew assembleDebug
npm run build:android:release            # ./gradlew assembleRelease
```

Metro cache reset: `npm start -- --reset-cache`. Android clean: `cd android && ./gradlew clean`.

## Layout

**Entry**: `index.js` → `src/App.tsx` (wraps `GestureHandlerRootView` + `SafeAreaProvider` + `RootNavigator`). `app.json` registers `PhlixMobile`.

**`src/api/`** (axios + manager singletons): `client.ts` (`BASE_URL = 'https://api.phlix.app'`, 30s timeout, auth interceptor, 401 refresh; **`delete<T>(url, config?)` takes an OPTIONAL axios config [E10c] so a caller can send a request BODY on a DELETE — the documented `delete(url,{data})` form, used by `AdminManager.removeCatalogSource`; backward-compatible, every other `delete(url)` call is unchanged**) · `AuthManager.ts` · `LibraryManager.ts` (`getLibraries/getLibraryItems/browseMedia(params)/getLetterIndex(params?)/getRecentlyAdded/getMediaItem/getSeasons/getEpisodes/search`; `browseMedia` → GET `/media` → `{items,total,limit,offset}` modeling the FULL filter set [`BrowseMediaParams`: `search/genres[]/ratings[]/actors[]/companies[]/yearFrom/yearTo/match/sort/order/limit/offset/libraryId/parentId/topLevel`]; **`getLetterIndex(params?)` [E10b] → GET `/media/letter-index` → returns the WHOLE `{letters:[{letter, offset:CUMULATIVE, count}], total}` envelope** [no over-unwrap], taking the SAME `BrowseMediaParams` so the rail reflects active filters [non-alpha leading char folds into the `#` bucket server-side]. **ARRAY-PARAM SERIALIZATION: no `paramsSerializer` is set — axios's default [verified axios 1.18.1 `toFormData`] renders flat arrays as `genres[]=A&genres[]=B` [URL-encoded `genres%5B%5D=`], EXACTLY the server's `key[]=` shape; do NOT add a paramsSerializer.** camelCase scalars [`yearFrom`/`yearTo`/`libraryId`/`parentId`/`topLevel`] pass through verbatim) · `PlaybackManager.ts` · `TranscodeManager.ts` (`startTranscode(id, profile?)` [honours `?profile=`] + `prepare()` polling helper that resolves the transcode-fallback path `PlayerScreen` uses on a direct-play `onError`; **`prepare()`'s `PrepareResult` carries `variants: Rendition[]`** [G3] — the ABR quality ladder [server A7], remembered off either the start response or the first status poll that carries a non-empty list [`[]` on a legacy/pre-ABR server] — this transcode job is the ONLY source of a real, pinnable variant ladder in this client, since `MarkerManager.getPlaybackInfo` returns markers only, not a ladder) · `ProfileManager.ts` (multi-user profiles; **admin-gated** `/admin/users/{userId}/profiles` + `/admin/profiles/{id}` + `/admin/profiles/{id}/pin`, bare envelopes) · `AdminManager.ts` (**admin-gated** dashboard/stats/users/libraries + plugins/auth-providers/server-settings [E10c]; **PER-CONTROLLER envelopes** — `/admin/dashboard/*` is `{success,data,count}`, `/admin/stats/*` is `{data}`, `/admin/users*` + `/libraries*` are BARE `{users}`/`{user}`/`{libraries}`/`{library}`/`{message}`. **E10c additions:** **Plugins** [BARE `{plugins}`/`{plugin}`/`{sources}`] — `getPlugins()`→`.plugins`; `getPlugin(name)`→`.plugin` [name `encodeURIComponent`'d, like all plugin/provider name segments]; `installPlugin(url)` POST `/admin/plugins/install` body `{url}`→`.plugin` [201]; `updatePluginSettings(name, settings)` PUT `.../{name}/settings` body `{settings}`→`.plugin` [the WHOLE settings map is PUT]; `enablePlugin`/`disablePlugin` POST→`.plugin`; `uninstallPlugin(name)` DELETE→void [204 empty]; `getPluginCatalog()`→the WHOLE `{default_source, sources, catalogs, errors}` [exported `CatalogResponse`, no over-unwrap]; `addCatalogSource(url)` POST `.../catalog/sources` body `{url}`→`.sources`; `removeCatalogSource(url)` **DELETE-with-BODY** `delete(url, {data:{url}})`→`.sources`. **Auth-providers** [BARE] — `getAuthProviders()`→`.providers`; `enableAuthProvider`/`disableAuthProvider` POST→the WHOLE `{name, enabled, message}` [exported `AuthProviderToggleResult`]; `getAuthProviderConfigSchema(name)`→`.schema` [config is READ-ONLY — there is NO config WRITE route]. **Server settings** [ENVELOPED] — `getServerSettings()` GET `/admin/settings`→**unwraps `.data`**; `updateServerSettings(settings)` PUT body `{settings}`→**unwraps `.data`** [both wire `{success, data:ServerSettings}`]. All E10c body field names snake_case verbatim. **E10d additions:** **Backup** [ENVELOPED `{success, data|message, count?}`] — `createBackup(label?)` POST `/admin/backup/create` body `{label}` when given else `{}`→**unwraps `.data`** [`Backup`]; `listBackups()` GET `/admin/backup/list`→**unwraps `.data`** [`Backup[]`]; `deleteBackup(id)` DELETE `/admin/backup/{id}`→void [id `encodeURIComponent`'d, like all backup id segments]; `restoreBackup(id)` POST `.../{id}/restore`→void [500 on failure]; `uploadBackupS3(id)` POST `.../{id}/upload-s3`→void [500 on failure]; `getBackupSchedule()` GET `/admin/backup/schedule`→**unwraps `.data`** [`BackupSchedule`]; `updateBackupSchedule(input)` PUT `.../schedule` body = `input` verbatim [partial — both fields optional]→**unwraps `.data`**. **Logs** [BARE — NOT enveloped] — `getLogFiles()` GET `/admin/logs`→`.files` [`LogFile[]`]; `tailLog(file, lines?)` GET `/admin/logs/tail` params `{file}` + `{lines}` only when given→the WHOLE `{file, lines, truncated}` [`LogTail`]; `tailAllLogs(lines?)` GET `/admin/logs/tail-all` [params undefined when no lines]→the WHOLE `{files, lines, truncated}` [`LogTail`]. **FS browse** [ENVELOPED] — `browseFs(path?)` GET `/admin/fs/browse?path=` [undefined params for empty/absent path = roots]→**unwraps `.data`** [`FsListing`]. All E10d body field names snake_case verbatim) · `CastManager.ts` (**SERVER-MEDIATED Cast**, 4 backends Chromecast/Roku/AirPlay/DLNA; `discover()` = fault-tolerant fan-out of `/cast/devices`+`/roku/devices`+`/airplay/devices`+`/dlna/renderers`, each leg swallows error→[] so one dead backend never blanks the others; `castTo/pause/resume/stop/seek/status` dispatch per backend; **request bodies are EXACT snake_case** — `media_url`/`audio_url`/`uri`, `mime_type`/`content_type`, `media_item_id`, `position_ms`/`position_ticks` [DLNA ticks = ms × 10000]; **capability gates no-op unsupported transports** — DLNA resume, Roku stop+seek, AirPlay seek; **NO native Cast SDK** — all over HTTP, native SDK is a future enhancement) · `LiveTvManager.ts` (**ADMIN-gated** Live TV / EPG / DVR [E8]; routes start `/admin/livetv`; BARE `{success,<key>}` envelopes — channels→`{channels}`/`{channel}`, guide→`{programs}`/`{program}`, recordings→`{recordings}`/`{recording}`, series-rules→`{rules}`; `getChannelStreamUrl(id)` resolves the 302 stream redirect via a RAW `fetch(absoluteUrl,{redirect:'manual'})` reading the `location` header, falling back to the stream-endpoint URL on opaque redirect — uses `getApiBaseUrl()`+`buildRequestHeaders()` re-exported from `client.ts`; exports `isNotConfiguredError(err)` = axios 404/500 → "no tuner") · `MusicManager.ts` (**Bearer-gated** music browse [E9a]; routes start `/music`; `getArtists/getArtist(name)/getAlbums/getAlbum(name)/getTracks({limit,offset})/getTrack(id)/getNowPlaying()`; BARE envelopes `{artists}`/`{artist}`/`{albums}`/`{album}`/`{tracks,total,limit,offset}`/`{track}`/`{now_playing|null}`; `getTracks` returns the WHOLE `{tracks,total,limit,offset}` envelope [exported `TracksResponse`], the rest unwrap to the inner value; **artist/album path segments are NAMES not ids** → `encodeURIComponent`'d; aggregates across ALL music libraries server-side [no library_id]. **LANDMINE: `getArtists/getAlbums/getTracks/getTrack` return FLAT formatTrack-normalized rows, but `album.tracks` from the album endpoints are RAW media-item rows** whose track fields live under `metadata.*` → normalize each via `normalizeAlbumTrack` from `types/music`. Music playback REUSES the existing Player — a track `id` IS a media_item id, so navigate `Player({itemId: track.id})`; there is NO dedicated music stream route. now-playing `position` is inferred 100ns ticks, display-only) · `PhotoManager.ts` (**Bearer-gated** photo browse [E9b]; routes start `/photo` **(SINGULAR)**; `getAlbums(libraryId)/getAlbum(albumId,libraryId)/getPhotos(libraryId,{limit,offset})/getPhoto(id)/getSlideshow(libraryId,{albumId,interval})`; envelopes `{albums}`/`{album}` unwrap to the inner value, `getPhoto`→`.photo`, while `getPhotos` returns the WHOLE `{photos,pagination:{limit,offset,count}}` envelope [exported `PhotosResponse`] and `getSlideshow` the WHOLE `{slideshow,interval}` envelope [exported `SlideshowResponse`]; **`library_id` is a REQUIRED query param** on albums/album-{id}/photos/slideshow [always sent first; only the truly optional limit/offset/album_id/interval are omitted-when-undefined]; `albumId` = md5 of the album date. **KEY: payloads carry SIGNED `thumbnail_url`/`full_url`** [token embedded] → fed straight into `<Image source={{ uri }}>` with NO Bearer header; the manager NEVER builds URLs and NEVER hits the thumbnail/full BYTE routes directly. Mirrors MusicManager doc/style; snake_case verbatim) · `CollectionManager.ts` (**Bearer-gated** [NOT admin-gated] collections CRUD + item management [E10a]; routes start `/collections`; `getCollections()/getCollection(id)/createCollection(input)/updateCollection(id,input)/deleteCollection(id)/addItem(id,mediaItemId)/removeItem(id,mediaItemId)/bulkAdd(id,ids)/refresh(id)/getLibraryCollections(libraryId)`; envelopes `{collections}`/`{collection}` unwrap to the inner value [`getCollections`/`getLibraryCollections`→`.collections`; `create`/`update`→`.collection`], while `getCollection` returns the WHOLE `{collection, items}` envelope [exported `CollectionWithItems`] and `bulkAdd` the WHOLE `{message, added_count}` envelope [exported `BulkAddResult`, body `{media_item_ids}`]; `deleteCollection`/`addItem`/`removeItem`/`refresh` are void [`{message}` discarded]; create body field names are snake_case verbatim [`library_id`/`smart_playlist_id`/`parent_id`/`sort_order`/`media_item_ids`] — do NOT camelCase. **Smart collections [`is_smart`] are refresh-only** — they're evaluated from a saved smart-playlist server-side and can't have items hand-added/removed [the manager has no client-side gate; the store/screens enforce it]. A collection groups media items within ONE library [`library_id`]. Errors bubble — the store owns the try/catch) · `FavoritesManager.ts` (**Bearer-gated** favorites/ratings [E10 favorites]; hits the NEW server favorites/ratings API, which is **account-level** [per-USER, not per-profile]; routes start `/media`//`/users`; `setFavorite(id)` POST `/media/{id}/favorite`→void; `removeFavorite(id)` DELETE `/media/{id}/favorite`→void; `setRating(id,n)` PUT `/media/{id}/rating` body `{rating}` [integer 1–10; server 400s out-of-range/non-numeric, 404 missing]→void; `clearRating(id)` DELETE `/media/{id}/rating`→void; `getFavorites({limit?,offset?})` GET `/users/me/favorites`→the WHOLE `{items,limit,offset}` envelope [exported `FavoritesResponse`, each item carries a `user_data:{favorite,rating}` block; undefined limit/offset omitted]. The `{message}` write envelopes are discarded → the mutators resolve `void`; ids `encodeURIComponent`'d; snake_case verbatim [`user_data`/`rating`]. The detail item's favorite/rating state is READ off `MediaItem.user_data` from `LibraryManager.getMediaItem` — this manager only MUTATES + lists. Errors bubble — the store/screen owns the try/catch + optimistic revert. MusicManager doc/style) · `WebAuthnManager.ts` (**passkeys / WebAuthn** [E10e]; the 6 `WebAuthnController` routes EXACT, snake_case verbatim; **TWO auth contexts** — `registerOptions(username?)` POST `/auth/webauthn/register/options` [**BEARER**, body `{username}` or `{}`]→raw `CreationOptions` JSON, `registerVerify(credential, challenge)` POST `.../register/verify` [**BEARER**, body `{credential, challenge}`]→`{credential_id, message}`, `loginOptions(username)` POST `/auth/webauthn/login/options` [**PRE-AUTH / passwordless**, body `{username}`]→raw `RequestOptions` JSON, `loginVerify(username, credential, challenge)` POST `.../login/verify` [**PRE-AUTH**, body `{username, credential, challenge}`]→`{access_token, refresh_token, user}` = the SAME token envelope password login returns, `getCredentials()` GET `/me/webauthn/credentials` [BEARER]→`.credentials` [`PasskeyCredential[]`], `deleteCredential(id)` DELETE `.../credentials/{id}` [BEARER, id `encodeURIComponent`'d]→void [`{message}` discarded]. The options endpoints return the server's RAW WebAuthn options [binary fields base64url]; the service hands that object as a JSON string straight to the native authenticator and posts the authenticator's response back to verify — **JS never touches the base64url plumbing**. Errors bubble — the service/store owns the try/catch) · `index.ts` re-exports.

**`src/stores/`** (Zustand): `useAuthStore.ts` · `usePlayerStore.ts` · `useLibraryStore.ts` · `useSettingsStore.ts` (**`defaultQuality`** [persisted playback-quality preference, default `'auto'`] is now genuinely READ + APPLIED [G3] — it was persisted/settable from day one but never read by any playback code until `PlayerScreen` started seeding its quality-picker selection from it via `seedQualitySelection` and persisting a fresh pick back via `setDefaultQuality`) · `useProfileStore.ts` (profiles list + active profile; persists active id to `phlix_active_profile_id`) · `useAdminStore.ts` (admin dashboard + users + libraries state & actions; section-scoped loading/error; loaders swallow into error, mutators rethrow. **E10c adds 3 sections:** plugins [`plugins`+`pluginsLoading/Error`, `catalog`+`catalogLoading/Error`], authProviders, serverSettings. Loaders `loadPlugins`/`loadCatalog`/`loadAuthProviders`/`loadServerSettings` swallow→section error. Mutators rethrow AND optimistically patch local state **only AFTER `await` succeeds** [no pre-await write → a failed mutation throws before any local change, no rollback needed]: `enable`/`disablePlugin` flip `enabled` on the row, `uninstallPlugin` filters it out, `updatePluginSettings` merges the refreshed detail into the row, `install`/`add`/`removeCatalogSource` reload after mutation, `enable`/`disableAuthProvider` reload the list, `updateServerSettings` replaces `serverSettings` with the unwrapped result. **E10d adds 3 sections:** backups [`backups`+`backupsLoading/Error`, `backupSchedule`+`backupScheduleLoading/Error`], logs [`logFiles`+`logFilesLoading/Error`, `currentTail`+`tailLoading/Error`], fs [`fsListing`+`fsLoading/Error`, plus `fsPickedPath`]. Loaders `loadBackups`/`loadBackupSchedule`/`loadLogFiles`/`tailLog`/`tailAllLogs`/`browseFs` swallow→section error. Mutators rethrow: `createBackup` reloads the list + returns the new record, `deleteBackup` filters the row out, `restoreBackup`/`uploadBackupS3` rethrow [no list change], `updateBackupSchedule` replaces `backupSchedule`. **`fsPickedPath` is the directory-picker hand-off** — `setFsPickedPath(path)`/`clearFsPickedPath()` are the serializable setters AdminFsBrowse [pick mode] and AdminLibraries use to pass a chosen folder back WITHOUT a nav callback) · `useCastStore.ts` (cast devices/discover state + activeDevice/activeSession/transport; `discover/castTo/pause/resume/stop/seek/refreshStatus`; `disconnect()` clears the session WITHOUT stopping the device [remote keeps playing — console Esc behavior], `stopAndDisconnect()` stops then clears; the 2.5s status-poll interval is owned by `CastScreen`, not the store) · `useLiveTvStore.ts` (Live TV channels/guide/recordings/upcoming/series-rules state with section-scoped loading/error; `notConfigured` flag set when ANY loader hits a 404/500 [`isNotConfiguredError`] so screens show "not set up" not a raw error; loaders swallow→error/notConfigured, mutators [updateChannel/refreshGuide/createRecording/deleteRecording] rethrow; `getChannelStreamUrl` delegates to the manager) · `useMusicStore.ts` (music browse [E9a]: `artists`/`albums`/`tracks` [first page only] + `nowPlaying`, each section-scoped `*Loading`/`*Error`; loaders `loadArtists/loadAlbums/loadTracks/loadNowPlaying` swallow→section error; NO mutators [music is read-only]; `initialState`+`reset()`. Album-level track listing is screen-local — `MusicAlbumScreen` calls `musicManager.getAlbum` directly, not the store; the Tracks tab paginates screen-locally too, the store only holds page 1) · `usePhotoStore.ts` (photo browse [E9b]: `photoLibraries`/`selectedLibraryId`/`albums`/`currentAlbum`, each with section-scoped `*Loading`/`*Error`; `setSelectedLibraryId` mutator; loaders `loadPhotoLibraries` [reuses `libraryManager.getLibraries()` filtered to `type==='photo'`, AUTO-SELECTS `selectedLibraryId` ONLY when exactly one photo lib — multiple keeps the existing selection so a deep-link/tap isn't clobbered], `loadAlbums(libraryId)`, `loadAlbum(albumId,libraryId)` — all swallow→section error; NO mutators beyond the selection setter [photos are read-only]; `initialState`+`reset()`. Per-album photo lists are screen-local — `PhotoAlbumScreen`/`PhotoViewerScreen` call `photoManager.getAlbum` directly, not the store) · `useCollectionStore.ts` (collections [E10a]: `collections` [full list] + `currentCollection`/`currentItems` [the open collection], each section-scoped `collectionsLoading`/`collectionsError` and `currentLoading`/`currentError`; loaders `loadCollections`/`loadCollection(id)` swallow→section error; mutators `createCollection`(reloads the list so the new row appears in order)/`updateCollection`(splices the updated row into both `collections` and `currentCollection`)/`deleteCollection`(filters the list + clears `currentCollection`/`currentItems` when it was open)/`addItem`/`removeItem`/`refresh`(reload the open collection so its items reflect the change) all set error AND rethrow [mirrors useLiveTvStore]; `initialState`+`reset()`) · `useSearchStore.ts` (advanced search + letter rail [E10b]: `query`+`filters` [`MediaFilters` UI state] + `results`/`total`/`offset` + `loading`/`error`, plus `letterIndex`/`letterIndexLoading`/`letterIndexError`; `SEARCH_PAGE_SIZE = 50`. `setQuery`/`setFilters` plain setters; `search(reset=true)` paginates `browseMedia(buildBrowseParams(query,filters))` — reset REPLACES from offset 0, else APPENDS at the current `offset` [reentrancy-guarded via `loading`; `offset` advances by actual `items.length`, total falls back to `items.length`]; `loadLetterIndex()` calls `getLetterIndex` with the SAME built params [so rail offsets align with the rendered set]; `jumpToLetter(letter)` no-ops if the letter is absent or a load is in flight, else feeds the bucket's CUMULATIVE `offset` straight into `browseMedia({...,offset})` as the server `?offset=` [NOT a list index] and REPLACES results, then continues the single linear cursor; loaders swallow→`error`/`letterIndexError` [mirrors useMusicStore]; `initialState`+`reset()` force-clears `filters:{}`) · `useFavoritesStore.ts` (favorites LIST [E10 favorites]: `items`/`offset`/`hasMore`/`loading`/`error`; `FAVORITES_PAGE_SIZE = 50`. `loadFavorites(reset=true)` paginates `favoritesManager.getFavorites` — reset REPLACES from offset 0, else APPENDS the next page [reentrancy-guarded via `loading`; `offset` advances by actual `items.length`; `hasMore = page.length >= PAGE_SIZE` so a short page ends pagination]; the LOADER swallows→`error`. Mutators `toggleFavorite(id,next)`/`rate(id,n)`/`clearRating(id)` proxy the manager, set `error` AND RETHROW [the MediaDetailScreen owns single-item optimistic state]; `toggleFavorite(false)` ALSO filters the un-favorited row out of the list so it disappears. `initialState`+`reset()`) · `useWebAuthnStore.ts` (passkey credential management [E10e]: `credentials`+`loading`+`error`; `loadCredentials()` swallows→`error`; mutators `registerPasskey(name?)` [delegates to `WebAuthnService`, reloads the list + returns the result] and `deletePasskey(id)` [filters the row out] set `error` AND RETHROW [the screen owns the Alert]; `initialState`+`reset()`. The actual passwordless LOGIN ceremony does NOT live here — it sets `useAuthStore` directly via `WebAuthnService`) · `index.ts` re-exports. Pattern: `create<State>((set, get) => ({ ...initial, ...actions }))`.

**`src/screens/`**: `HomeScreen.tsx` · `MediaDetailScreen.tsx` (**E10 favorites adds an interactive favorite + rating control**: the static `★ rating` badge is replaced with a ♥/♡ favorite toggle + a 5-star [each star = 2 points → 1–10] rating row, shown ONLY when `mediaItem.user_data` is present [authenticated; `user_data` is `null` for guests → control hidden]. Seeds local optimistic state [`isFavorite`/`rating`] from `mediaItem.user_data` in `loadMediaDetails`; a tap snapshots the prior value → sets the new value locally → calls `FavoritesManager` DIRECTLY [not via the store, to keep the single-item optimistic state self-contained] → REVERTS to the snapshot + Alerts on error; per-control busy guards [`favBusy`/`rateBusy`] prevent double-fire. Re-tapping the star matching the current rating CLEARS it [`isClearTap` → DELETE rating]. Existing play/resume/cast/download/series rendering untouched) · `LibraryScreen.tsx` (ENTRY-POINT router: `handleLibraryTabPress` sends `type === 'music'` libs to `Music` [E9a] and `type === 'photo'` libs to `Photos` [E9b] WITHOUT selecting them; video/series libs use the existing items grid; the boot auto-select `firstBrowsable` filter SKIPS BOTH `'music'` and `'photo'` so neither is ever loaded into the items grid, and the no-selection EmptyState copy is generalized to cover music+photo-only servers) · `MediaDetailScreen.tsx` · `PlayerScreen.tsx` (**G3 adds an in-app quality picker**: a `QualityMenu` [pill button in the top bar, shown only when `qualityOptions.length > 1`] backed by `buildQualityOptions(qualityVariants, qualityMasterUrl)` off `src/components/player/quality.ts`; `qualityVariants` populate ONLY once a transcode fallback resolves [server A7 `variants[]`, via `TranscodeManager.prepare()`] — direct-play success leaves `qualityVariants=[]` so the menu never shows for natively-playable files. The initial pick is seeded from the persisted `useSettingsStore.defaultQuality` via `seedQualitySelection` [falls back to `'auto'` when the saved rung isn't in the current ladder]; `handleQualitySelect` persists a fresh pick back via `setDefaultQuality` and swaps the player `src` [master playlist for `'auto'`, the rung's own `media_v{id}.m3u8` for a pin]. **Position preservation across a quality swap**: native players reload from the `startPosition` prop on a `src` change, so a `currentPositionRef` mirrors the live playback clock [updated from every `setCurrentTime` source] and `handleQualitySelect` reads it to seed a NEW `playerStartPosition` state at switch time — `playerStartPosition` [not the mount-time `startPosition` route param] is what's bound to the `<PhlixPlayerView startPosition=...>` prop, so the swap resumes near the live position instead of restarting from 0. A native `maxResolution` track-selection prop was evaluated and intentionally skipped — redundant with the variant-URL pin, which already hard-caps resolution with zero native code; flagged as a possible future native enhancement) · `SearchScreen.tsx` (advanced search + A–Z rail [E10b]; now **store-backed** [`useSearchStore`], was local state. Keeps the SearchBar [submit → `performSearch`] so basic text search is unchanged. Adds a **Filters** toolbar button [active-dot when filters set] → a bottom-sheet Modal of genres [comma-separated free-text TextInput — no genre catalog endpoint in scope], rating multi-select chips [fixed `RATING_OPTIONS`], year from/to number inputs, sort chips [`SORT_OPTIONS`] + an asc/desc order toggle; uses DRAFT state committed on Apply [Cancel/X discard] so partial edits don't fire searches. Adds the right-side **A–Z letter rail** [`jumpToLetter`, zero-count/absent letters greyed-disabled via `activeLetters`] shown ONLY in browse-mode [no text query] AND name-sorted [`sort` undefined or `'name'`] AND results>0 — because the server IGNORES `topLevel`/letter-index alignment once `search` is set. Results FlatList [2-col MediaCard] with load-more [`onEndReached` gated `!loading && results.length < total`] + tap → `MediaDetail {itemId}`; loading/error/empty/initial states, dark palette) · `SettingsScreen.tsx` · `DownloadsScreen.tsx` · `LoginScreen.tsx` (**E10e adds a passwordless "🔑 Sign in with a passkey" button** shown ONLY when `WebAuthnService.isSupported` resolves true — reuses the existing username + server-URL fields, persists `serverUrl` BEFORE the ceremony [so the pre-auth options/verify calls target the right host], then `WebAuthnService.loginWithPasskey(username)` which runs the native assertion ceremony, posts to `login/verify`, saves the returned token envelope via `AuthManager.savePasskeyLogin` + flips `useAuthStore` so RootNavigator swaps Login→Main exactly like password login; busy-guarded; does NOT regress password login) · `ProfileSelectScreen.tsx` (admin-only profile manager; reached via Settings → Profiles, registered in the root stack — NOT the tab bar) · `AdminDashboardScreen.tsx` (admin-only server overview: now-playing/top-users/top-media/storage/activity; reached via Settings → Admin → Dashboard, registered in the root stack; non-admins see an "Admin access required" state) · `AdminUsersScreen.tsx` (admin-only user management: status-filter chips, approve/reject/disable/set-admin/reset-password[shown once]/delete[surfaces 400 self/last-admin], create + edit modals; Settings → Admin → Users) · `AdminLibrariesScreen.tsx` (admin-only library management: add/edit/delete, Scan/Rescan/Match-metadata with a live `getScanStatus` poll [interval started on trigger, ticks all active libraries, stops a library on terminal `completed`/`failed`/null status, full teardown on unmount], per-library scan-history sheet; Settings → Admin → Libraries. **E10d wires the real paths picker** [the E6b-deferred TODO]: a "Browse…" link on the paths field navigates to `AdminFsBrowse {mode:'pick'}`, and a `useEffect` consumes `fsPickedPath` [appends it deduped to the draft paths via `parsePathsInput`/`pathsToInput`] then `clearFsPickedPath()` so it's consumed exactly once — add/edit/scan flows otherwise untouched). · `AdminPluginsScreen.tsx` (admin-only plugin management [E10c]; segmented **Installed | Catalog**. Installed: an "Install from URL" box [gated by `validatePluginUrl` — https://|file:// only] as the FlatList header + plugin rows [enable/disable Switch, Settings → a settings-schema editor modal, Uninstall w/ confirm Alert]. The settings editor builds a form from `pluginSettingFields(detail.settings_schema)` — bool→Switch, number→numeric input, string→input, **SECRET fields render `secureTextEntry`** — seeds from `detail.settings`, coerces each value by kind on Save and PUTs the WHOLE settings map; "no configurable settings" when the schema is empty. Catalog: add-source box + sources list [the `default_source` is not removable, others Remove w/ confirm → DELETE-with-body], per-source error rows, per-catalog plugin lists. Settings → Admin → Plugins) · `AdminAuthProvidersScreen.tsx` (admin-only auth providers [E10c]; provider list with an enable/disable Switch [toggle position uses an explicit `enabled` if the payload carries one, else falls back to `supports_authentication`; the list reloads after each toggle so it self-corrects] → "View config schema" opens a READ-ONLY JSON viewer modal with a "configure on the server, no mobile write route" note; Settings → Admin → Auth Providers) · `AdminServerSettingsScreen.tsx` (admin-only server settings [E10c]; a ScrollView of editable rows typed off the `types` map [bool→Switch, string/number→input], an **OVERRIDDEN badge** on overridden keys, Save sends a PATCH of **ONLY changed keys** [coerced; no-op Alert when nothing changed] so untouched/derived keys are never re-asserted; handles the `{success,data}` envelope via the manager unwrap; Settings → Admin → Server Settings) · `AdminBackupScreen.tsx` (admin-only backup management [E10d]; a FlatList of backups [label/id, size via `formatBackupSize`, created_at] whose ListHeader carries a "+ Create backup" [optional-label modal] action + a **Schedule card** [interval-days + retention-count inputs seeded from the loaded schedule via an effect, Save validates via `validateScheduleInput` and sends ONLY non-empty fields = partial PUT]. Per-row **Restore** [destructive confirm — copy warns it OVERWRITES the DB/config and may restart the server], **Delete** [destructive confirm], and **Upload to S3** [Alert on success/fail]; all 500/404 surfaced via Alert; Settings → Admin → Backup) · `AdminLogsScreen.tsx` (admin-only log viewer [E10d]; a two-pane flow in ONE screen keyed off local `selected` state [NOT a nav push]: a file-list view [per-file size + modified, plus an "All logs (merged)" row using the `__all__` sentinel] ↔ a tail view [monospace horizontally-scrollable line list so long lines aren't clipped, line-count chips 200/500/1000 that re-tail on change via `clampLogLines`, a "truncated" badge when set, pull-to-refresh re-tails the current selection, "‹ Logs" back]. Single-file → `tailLog`; the merged row → `tailAllLogs`; Settings → Admin → Logs) · `AdminFsBrowseScreen.tsx` (admin-only directory browser [E10d]; horizontal tappable breadcrumb of `breadcrumbs(listing.path)`, "‹ Up one level" using `listing.parent`, a FlatList of dir entries → tap descends via `browseFs`. **Doubles as a path picker**: in `mode:'pick'` it shows a "Select "<path>"" button [disabled at roots where `path` is null] that calls `setFsPickedPath(listing.path)` + `navigation.goBack()`; Settings → Admin → File Browser, and reached as the AdminLibraries Browse picker). Pure screen logic (status filter, paths parse, `isJobActive` poll predicate, badge; plus E10c helpers `settingKindForType`/`isSecretField`/`pluginSettingFields`/`validatePluginUrl`/`settingFieldType`/`coerceSettingValue`/`settingValueToInput`/`isOverridden` + exported `SettingFieldKind`/`PluginSettingField`; plus E10d helpers `formatBackupSize` [reuses `formatFileSize`; invalid/negative/NaN→em-dash], `validateScheduleInput(days,count)` [empty=unchanged/valid; days int≥0, count int≥1], `parentPath(path)` [trailing-slash-tolerant parent], `breadcrumbs(path)` [cumulative crumbs incl. a synthetic root for absolute paths], `clampLogLines` [1–2000, default 200] + `LOG_LINE_OPTIONS`=[200,500,1000] + `LogLineCount`) is extracted to `src/screens/admin/adminScreenHelpers.ts` and unit-tested in `src/screens/admin/__tests__/`. · `CastScreen.tsx` (server-mediated Cast; takes route PARAMS `{mediaItemId, streamUrl, title?, thumbnail?, durationSecs?}`; discovery picker [FlatList grouped/labeled by backend, pull-to-refresh, "No cast devices found" empty state] → on select casts the item → transport view [status badge, Play/Pause, Stop if `canStop`, Seek bar if `canSeek`] with a 2.5s status poll keyed off `activeDevice` via refs, fully torn down on unmount + when the device clears; **Back leaves the device playing** — only Stop halts it; reached via the Cast button on MediaDetailScreen) · `LiveTvScreen.tsx` (**admin-gated** [E8]; non-admin → "Admin access required"; `notConfigured` → friendly "Live TV is not set up" EmptyState; channel list with a current-program "Now:" label from the guide, expandable per-channel EPG, pull-to-refresh + a "Refresh guide" action; tap a channel → resolve stream URL → navigate to `Player` with `streamUrl`+`liveTitle`; reached via Settings → Admin → Live TV) · `LiveTvRecordingsScreen.tsx` (admin-gated DVR; upcoming + all recordings, create-from-program picker modal, delete-with-confirm, series-rules list-only; reached via Live TV → Recordings). · `MusicScreen.tsx` (music browse [E9a]; takes `{libraryId?}`; segmented **Artists | Albums | Tracks** [custom dark segmented row]; Artists → tap sets in-screen `selectedArtist` → that artist's albums filtered via `albumsForArtist` with a "‹ All artists" back row; Albums → tap navigates `MusicAlbum`; Tracks → paginated 50/page screen-locally via `getTracks` offset → tap navigates `Player({itemId})`; per-section LoadingSpinner/ErrorView/EmptyState; reached when a `type === 'music'` library is tapped on LibraryScreen) · `MusicAlbumScreen.tsx` (reads `albumName` param, `getAlbum(name)`, maps `album.tracks` through `normalizeAlbumTrack` THEN `sortTracks`, header [name + `albumSubtitle`] + ordered position•name•subtitle rows; track tap → `Player({itemId})`). Pure music screen logic (`MUSIC_SEGMENTS`, `albumsForArtist`, `trackSubtitle`, `artistSubtitle`, `albumSubtitle`, `trackPositionLabel`) is extracted to `src/screens/music/musicScreenHelpers.ts` and unit-tested in `src/screens/music/__tests__/`. · `PhotosScreen.tsx` (photo browse [E9b]; optional `{libraryId?}` route param — when present used directly, when absent runs `loadPhotoLibraries` then: none → EmptyState, exactly one → store auto-selects + screen adopts it, many → a simple library picker list; once a library is chosen renders a 2-col album grid [`cover_photo?.thumbnail_url` with placeholder fallback, title via `albumGridTitle`, `photoCountLabel` badge]; tap → `navigation.navigate('PhotoAlbum', {albumId, libraryId, title})`; reached when a `type === 'photo'` library is tapped on LibraryScreen) · `PhotoAlbumScreen.tsx` (reads `{albumId, libraryId, title}`, `photoManager.getAlbum`, 3-col thumbnail grid [`thumbnail_url`]; tap → `navigation.navigate('PhotoViewer', {libraryId, albumId, startIndex})` — passes only the serializable index, NEVER the photo array) · `PhotoViewerScreen.tsx` (reads `{libraryId, albumId, startIndex?}` and **RE-FETCHES the album itself** via `getAlbum(albumId, libraryId)` — no arrays through nav params; full-screen black horizontal `pagingEnabled` FlatList of `full_url` images [`getItemLayout` by screen width, clamped `initialScrollIndex`, `onScrollToIndexFailed` retry]; tap toggles a bottom EXIF info overlay built from `formatExifSummary` — EXIF is LAZY [list `metadata` shown instantly, then refined by a cancellable `getPhoto(focusedId)`, best-effort: a failed fetch keeps the list metadata]; a "▶ Slideshow" button fetches the interval via `getSlideshow(libraryId,{albumId,interval})` then auto-advances over the already-loaded album photos via a ref'd `setInterval` [reads `indexRef`/`lenRef` so it never goes stale, cleared on Stop + unmount, `nextSlideIndex` wraps]). Pure photo screen logic (`pickInitialLibrary`, `albumGridTitle`, `photoCountLabel`, `clampIndex`, `nextSlideIndex`) is extracted to `src/screens/photos/photoScreenHelpers.ts` and unit-tested in `src/screens/photos/__tests__/`. · `CollectionsScreen.tsx` (collections list [E10a]; FlatList of all collections [⚡ prefix + `collectionSubtitle` kind label "Smart"/"Collection" — list rows show a KIND not a count because `GET /collections` returns no per-collection item count], "+ New" header button → create modal [name TextInput + a library picker sourced from `libraryManager.getLibraries()`, auto-selects when exactly one library; validated via `validateCollectionInput`], tap a row → `CollectionDetail`, long-press → delete-with-confirm Alert; reached via Settings → Library → Collections) · `CollectionDetailScreen.tsx` (reads `{collectionId, title}`, `loadCollection`, header [name, ⚡ smart badge, a "Refresh" button shown ONLY for smart collections], 2-col MediaCard grid [reuses MediaCard like LibraryScreen] → tap navigates `MediaDetail`, a real item count via `collectionSubtitle(collection, items.length)` once loaded; remove-item ✕ overlay [confirm Alert] shown ONLY for manual collections [`canEditItems` = `!is_smart`]; pull-to-refresh re-loads the collection — **smart collections are refresh-only**). Pure collection screen logic (`isSmartCollection`, `canEditItems`, `collectionSubtitle`, `validateCollectionInput`) is extracted to `src/screens/collections/collectionScreenHelpers.ts` and unit-tested in `src/screens/collections/__tests__/`. · `FavoritesScreen.tsx` (favorites list [E10 favorites]; a 2-col MediaCard grid of `useFavoritesStore.items` with pull-to-refresh, load-more [`onEndReached` gated `!loading && hasMore && items.length>0`], a footer spinner, and an EmptyState ["No favorites yet"]; full-screen spinner on the initial empty load, ErrorView+retry on empty-error; tap → `MediaDetail {itemId}`; re-fetches on mount [`useEffect → loadFavorites(true)`] so navigating back from a detail un-favorite refreshes. Reached via **Settings → Library → Favorites** [mirrors the Collections row]). · `PasskeysScreen.tsx` (passkey management [E10e]; a FlatList of `useWebAuthnStore.credentials` [label via `passkeyLabel`, created date, last-used via `lastUsedLabel`] with pull-to-refresh, a "+ Add a passkey" header [optional-name modal → runs the native registration ceremony via `WebAuthnService.registerPasskey`], and per-row delete-with-confirm. **Gated on `WebAuthnService.isSupported`** — when no platform authenticator is present [or in Jest / before a native build] it renders a "Passkeys aren't available on this device" state instead of the list; loading/error/empty states wired; reached via **Settings → Security → Passkeys**). Pure favorites logic [E10 favorites] (`clampRating` [round + clamp 1–10, NaN→MIN, ±Inf→bound], `starsFromRating(rating)`→`StarFill[5]` [`'full'|'half'|'empty'`; null/undefined→5 empties; 2 points/star], `ratingForStar(starIdx)` [star N → N×2], `nextFavoriteState(cur)`, `isClearTap(starValue,cur)` [re-tap current rating = clear], consts MIN/MAX_RATING + STAR_COUNT + POINTS_PER_STAR + `StarFill` type) is extracted to `src/screens/favorites/favoritesHelpers.ts` and unit-tested in `src/screens/favorites/__tests__/`. Pure search-rail logic [E10b] (`RAIL_LETTERS` [`#` then A–Z, 27 entries], `offsetForLetter(index, letter)` [cumulative offset or null], `activeLetters(index)` [Set of count>0 letters]) plus re-exports of the `types/search` filter helpers [single source of truth] is extracted to `src/screens/search/searchScreenHelpers.ts` and unit-tested in `src/screens/search/__tests__/`. Pure passkey logic [E10e] (`passkeyLabel(cred)`, `lastUsedLabel(cred)` [never-used → "Never used"], `webauthnErrorMessage(err)` [maps the axios `{message}`/`{error}` envelope + native cancel/unavailable strings to a friendly message], `cleanUsername(raw)`) is extracted to `src/screens/webauthn/webauthnHelpers.ts` and unit-tested in `src/screens/webauthn/__tests__/`. All `default export`, wrapped in `<SafeContainer>`, use `useNavigation<NativeStackNavigationProp<any>>()`.

> **Live TV is ADMIN-gated + a REACH feature (no tuner on most servers).** Gated on `is_admin`; 404/500 from `/admin/livetv/*` is treated as "not configured", never surfaced raw. **No user-facing Live TV server route exists** (flag upstream). Stream playback resolves a 302 redirect to the tuner HLS URL — the resolved URL must be directly playable; if it still needs auth, the server should add a `GET .../stream-url` JSON or signed-livetv-url (flag upstream).

> **Profiles upstream gap:** server has no user-facing `/users/me/profiles` route — only the admin-gated routes above. Mobile profile management is admin-scoped (non-admins see an informational state). Add a user-facing route upstream for true multi-user mobile.

**`src/components/`**: `layout/SafeContainer.tsx` · `media/{MediaCard,PosterCard,MediaList,ContinueWatching}.tsx` · `player/{PlayerControls,SeekBar,SkipButton,QualityMenu}.tsx` (**`QualityMenu.tsx`** [G3]: bottom-sheet quality picker Modal, matches the subtitle-picker style/dark palette; thin declarative renderer — all logic lives in the pure sibling **`player/quality.ts`** [`buildQualityOptions`/`resolveQualityUrl`/`seedQualitySelection`/`activeQualityLabel`, consuming `Rendition`/`QualitySelection` DIRECTLY from `@phlix/contracts`, no locally-reinvented shape]) · `ui/{LoadingSpinner,ErrorView,EmptyState,SearchBar}.tsx` · `cast/CastButton.tsx` (named export; navigates to the `Cast` route with the item's id + signed `stream_url` [+ title/poster/duration]; wired into MediaDetailScreen's button row for non-series items that carry a `stream_url`). Each subdir has `index.ts` named re-exports.

**`src/navigation/RootNavigator.tsx`**: `NavigationContainer` → root `Stack` (`Login` | `Main` + `Profiles`/`AdminDashboard`/`AdminUsers`/`AdminLibraries`/`AdminPlugins`/`AdminAuthProviders`/`AdminServerSettings`/`AdminBackup`/`AdminLogs`/`AdminFsBrowse`/`LiveTv`/`LiveTvRecordings`/`Music`/`MusicAlbum`/`Photos`/`PhotoAlbum`/`PhotoViewer`/`Collections`/`CollectionDetail`/`Favorites`/`Passkeys`/`Cast`/`Player`) → `Tab` (`Home` · `Library` · `Search` · `Downloads` · `Settings`). `Cast` is a headered root-stack screen taking params; `Music` (title "Music", param `{libraryId?}`) and `MusicAlbum` (title "Album", param `{albumName}`) are headered root-stack screens [E9a]; `Photos` (title "Photos", param `{libraryId?}`) and `PhotoAlbum` (param `{albumId, libraryId, title?}`, title from `route.params.title` else "Album") are headered, while `PhotoViewer` (param `{libraryId, albumId, startIndex?}`) is registered with `headerShown:false` + `presentation:'fullScreenModal'` + black `contentStyle` [E9b]; `Collections` (title "Collections", no params) and `CollectionDetail` (param `{collectionId, title?}`, title from `route.params.title` else "Collection") are headered root-stack screens [E10a]; `Favorites` (title "Favorites", no params) is a headered root-stack screen [E10 favorites], reached from Settings → Library → Favorites; `Passkeys` (title "Passkeys", no params) is a headered root-stack screen [E10e], reached from the Settings → **Security** section; `AdminPlugins` (title "Plugins"), `AdminAuthProviders` (title "Auth Providers") and `AdminServerSettings` (title "Server Settings") are headered root-stack screens [E10c], reached from the Settings → Admin section (which gained Plugins / Auth Providers / Server Settings rows); `AdminBackup` (title "Backup", no params), `AdminLogs` (title "Logs", no params) and `AdminFsBrowse` (title "File Browser", param `AdminFsBrowse: { mode?: 'browse' | 'pick' } | undefined`) are headered root-stack screens [E10d], reached from the Settings → Admin section (which gained Backup / Logs / File Browser rows) — and `AdminFsBrowse {mode:'pick'}` is also pushed by AdminLibraries as the paths picker; `Player` presented as `fullScreenModal`. Auth gate reads `useAuthStore`.

**`src/types/`**: `media.ts` (`MediaItem`, `Library`, `Series`, `Season`, `Episode`, `UserData`. **E10 favorites:** `UserData` gained `rating?: number | null` [the user's 1–10 score; `null`, not absent, when unset — distinct from `MediaItem.rating`, the content-rating label STRING] + `favorite?: boolean`; `MediaItem.user_data?: UserData | null` [server sends `{favorite,rating}` on `GET /media/{id}` when authenticated, `null` for guests]. The single `UserData` interface is SHARED by continue-watching [`resume_position_ticks`/`is_watched`] and favorites — existing optional-chained `user_data?.resume_position_ticks` reads stay null-tolerant] · `cast.ts` (`CastBackend` union + normalized `CastDevice`/`CastStatus`/`CastSession`; PURE capability helpers `canResume`/`canStop`/`canSeek`; per-backend snake_case DTOs + `normalizeChromecast`/`normalizeRoku`/`normalizeAirplay`/`normalizeDlna`/`normalizeStatus`/`normalizeSession`) · `music.ts` (music domain [E9a]: `Artist`/`Track` [flat]/`RawAlbumTrack` [loose index-signature]/`Album` [`tracks` are RAW rows]/`NowPlaying`; PURE `normalizeAlbumTrack(raw): Track` [top-level field FIRST then `metadata.*` fallback, `name = metadata.title ?? raw.name ?? ''`, safe numeric coercion incl. numeric-string] + `sortTracks(tracks): Track[]` [stable disc→track, nulls last, new array]) · `photo.ts` (photo domain [E9b]: `PhotoExif` [snake_case, server-verbatim; summary fields strings, numerics/`*_or_null` nullable], `Photo` [album/list item: raw row + `metadata?: Partial<PhotoExif> & Record<string,unknown>` + SIGNED `thumbnail_url`/`full_url`], `PhotoDetail` [`metadata`+`exif` both full `PhotoExif`], `PhotoAlbum` [`id` md5(date), `date`, `photo_count`, optional `cover_photo`, `photos[]`], `SlideshowItem`; PURE helpers `albumTitle` [Unknown/empty → "Undated", else verbatim], `formatExifSummary(exif)` [ordered human lines — camera "make model", lens, "W×H" (needs BOTH dims), aperture, "ISO n", shutter, focal, formatted date, gps_display — skips null/empty, never throws], `gpsCoords(exif)` [null unless BOTH lat+lng finite]) · `collection.ts` (collections domain [E10a]: `Collection` [server-verbatim snake_case — `id`, `name`, `library_id`, nullable `smart_playlist_id`/`parent_id`, `sort_order:number`, `is_smart:boolean`, `created_at`/`updated_at`], `CreateCollectionInput` [`name`+`library_id` REQUIRED, rest optional], `UpdateCollectionInput` [fully-optional partial; nullable `smart_playlist_id`/`parent_id`]; interface-only, no runtime helpers — the testable logic lives in `collectionScreenHelpers.ts`) · `admin.ts` (admin domain; E6 user/library/dashboard/stats types + **E10c additions**: `Plugin` [installed-plugin row], `PluginSettingSchema` [`{type, required, secret, label, description}` — one `settings_schema` field], `PluginDetail` [adds `settings_schema` map + `settings`], `CatalogPlugin`, `PluginCatalog`, `CatalogError`, `CatalogResponse` [whole `{default_source, sources, catalogs, errors}`], `AuthProvider` [`{name, supports_authentication}` + index sig], `AuthProviderConfigSchema` [=`Record<string,unknown>` — read-only JSON-schema, NO write route], `ServerSettings` [`{settings, overridden, types}` — the unwrapped `.data`]; **E10d additions:** `Backup` [`{id, created_at, label, size}`], `BackupSchedule` [`{auto_backup_interval_days, retention_count, next_scheduled_backup, next_scheduled_backup_iso}`], `UpdateBackupScheduleInput` [both fields optional — partial PUT], `LogFile` [`{name, size, modified_at}`], `LogTail` [`{file?, files?, lines:string[], truncated:boolean}` — `file` set for single-file tail, `files` for tail-all], `FsEntry` [`{name, path}`], `FsListing` [`{path:string|null, parent:string|null, entries:FsEntry[]}` — null path/parent at roots]; all field names server-verbatim snake_case; free-form setting/schema VALUES are `unknown`) · `search.ts` (advanced-search + letter-index domain [E10b]: `LetterIndexEntry` [`letter`, `offset:CUMULATIVE`, `count`] / `LetterIndex` [`{letters, total}`, server-verbatim] / `MediaFilters` [advanced-filter UI STATE — all optional `genres`/`ratings`/`yearFrom`/`yearTo`/`sort`/`order`/`libraryId`], plus `RATING_OPTIONS` [G/PG/PG-13/R/NC-17/X/UNRATED] + `SORT_OPTIONS` [name/year/rating/date_added/runtime] const tuples → derived `RatingOption`/`SortOption`/`SortOrder` unions; PURE helpers `hasActiveFilters(f)`, `buildBrowseParams(query, filters)` [trims query, drops empty arrays/blank strings/undefined → clean `BrowseMediaParams`; year-0 safe via `typeof === 'number'`], `letterForName(name)` [uppercased first alpha A–Z else `#`; trims; empty→`#` — mirrors server `#` bucketing]. Imports `BrowseMediaParams` TYPE-ONLY from LibraryManager [no runtime cycle — LibraryManager imports `LetterIndex` type-only back]) · `webauthn.ts` (passkeys domain [E10e]: `PasskeyCredential` [server-verbatim snake_case `{id, created_at, last_used_at:string|null, name}`], `CreationOptions`/`RequestOptions` [the server's RAW WebAuthn options — named fields incl. the base64url `challenge` + an index signature for the raw passthrough], `PublicKeyRp`/`PublicKeyUser`, `RegistrationResult` `{credential_id, message}`, `AuthResult` `{access_token, refresh_token, user}`; interface-only) · `navigation.ts` (`RootStackParamList` incl. `Profiles` + `AdminPlugins`/`AdminAuthProviders`/`AdminServerSettings` [E10c — all `undefined` params] + `AdminBackup`/`AdminLogs` [E10d — `undefined`] + `AdminFsBrowse: { mode?: 'browse' | 'pick' } | undefined` [E10d — `pick` mode selects a folder back into AdminLibraries via `fsPickedPath`] + `Cast: {mediaItemId, streamUrl, title?, thumbnail?, durationSecs?}` + `Music: {libraryId?}` + `MusicAlbum: {albumName}` + `Photos: {libraryId?}` + `PhotoAlbum: {albumId, libraryId, title?}` + `PhotoViewer: {libraryId, albumId, startIndex?}` [E9b — serializable scalars only; PhotoViewer re-fetches the album by id, no photo arrays] + `Collections: undefined` + `CollectionDetail: {collectionId, title?}` [E10a — CollectionDetail re-fetches the collection by id, serializable scalars only] + `Favorites: undefined` [E10 favorites] + `Passkeys: undefined` [E10e], `TabParamList`, screen prop types) · `playback.ts` (`StreamInfo`, `DeviceProfile`, `SubtitleTrack`, `AudioTrack`, `PlaybackSession`. **G3:** re-exports `Rendition`/`RenditionId`/`QualitySelection` straight from `@phlix/contracts` [not redeclared], and the local `TranscodeJob`/`TranscodeStatus` gain `variants?: Rendition[] | null` [server A7 wire field; `null`/absent = legacy/pre-ABR server]) · `profile.ts` (`Profile`, `ProfileSettings`, `ContentRatingLabel`; `ratingToLabel`/`labelToRating` int↔label helpers — 0=G…6=UNRATED).

**`src/services/`**: `SecureStorage.ts` (`react-native-keychain`, service `com.phlix.mobile`) · `DownloadService.ts` · `NotificationService.ts` · `WebAuthnService.ts` (passkey ceremony orchestration [E10e]: `isSupported()` [→ native wrapper], `registerPasskey(name?)` [`registerOptions(name)` [BEARER] → `PhlixWebAuthn.register(JSON.stringify(opts))` → parse → `registerVerify(credential, opts.challenge)` echoing the original challenge], `loginWithPasskey(username)` [`loginOptions(username)` [PRE-AUTH] → `PhlixWebAuthn.authenticate(...)` → parse → `loginVerify(...)` → **`AuthManager.savePasskeyLogin(result)`** [persists the token envelope via the SAME AsyncStorage path password login uses] → flips `useAuthStore` [`setUser` + `isAuthenticated:true`] so RootNavigator swaps Login→Main], `listPasskeys`/`deletePasskey` [pure HTTP proxies]. The native ceremony runs the system biometric/PIN sheet — **device-only at runtime**) · `index.ts`.

**`src/utils/`**: `formatters.ts` (`formatTime`, `formatRuntime`, `formatFileSize`, `formatRelativeTime`, `truncateText`) · `storage.ts` (typed `AsyncStorage` wrapper).

**`src/native/types.ts`**: `PhlixPlayerInterface`, `PhlixDownloaderInterface`, `PlaybackEvent`, `DownloadEvent`, `PhlixWebAuthnInterface` [E10e — `isSupported()`/`register(optionsJson)`/`authenticate(optionsJson)`; **JSON-string in, JSON-string out**; base64url<->binary happens NATIVELY; must stay byte-for-byte aligned with the iOS Swift + Android Kotlin modules]. **`src/native/PhlixWebAuthn.ts`** [E10e] is the typed JS wrapper over `NativeModules.PhlixWebAuthn` — **guarded**: when the module is absent [Jest / before a native build] `isSupported()` resolves `false` [never throws] so the UI hides passkey affordances, while `register()/authenticate()` reject with `PASSKEYS_UNAVAILABLE_MESSAGE` so a bypassed gate fails loudly instead of crashing the bridge [mirrors the DownloadService guarded-module pattern].

**iOS native**: `ios/LocalPods/PhlixPlayer/PhlixPlayer.podspec` · `PhlixPlayerView.swift` (AVPlayer, KVO `status`, periodic time observer) · `PhlixPlayerViewManager.m` (`RCT_EXTERN_MODULE` bridge, props `src`/`autoPlay`/`startPosition`/`volume`/`muted`, methods `play`/`pause`/`seekTo`) · **`PhlixWebAuthn.swift` + `PhlixWebAuthn.m`** [E10e — passkeys; `@objc(PhlixWebAuthn) : NSObject` over `ASAuthorizationPlatformPublicKeyCredentialProvider` [**iOS 15+**, all `if #available(iOS 15.0, *)` guarded — below 15 → `isSupported` false / reject "unsupported"], an `ASAuthorizationController` + a `CeremonyDelegate` retained on `self`; does ALL base64url<->Data conversion IN Swift [decode challenge/user.id/allowCredentials[].id, encode rawId/clientDataJSON/attestationObject/authenticatorData/signature/userHandle] and returns the standard WebAuthn JSON string the server verify expects; `.m` is `RCT_EXTERN_MODULE` + `RCT_EXTERN_METHOD` for isSupported/register/authenticate. **The podspec's existing `*.{h,m,swift}` glob already picks up the new sources — no `source_files` edit; the podspec only ADDED `s.frameworks "...","AuthenticationServices"`.** Runtime is DEVICE-ONLY — the passkey ceremony shows a system biometric/PIN sheet that no simulator can run; CI compiles only].

**Android native**: `android/app/src/main/java/com/phlixmobile/MainActivity.kt` · `MainApplication.kt` · `player/PhlixPlayerPackage.kt` · `player/PhlixPlayerView.kt` (ExoPlayer, `@ReactProp` for `src`/`autoPlay`/`startPosition`/`volume`/`muted`/`rate`/`pictureInPicture`, `@ReactMethod` for control). Manifest: `android/app/src/main/AndroidManifest.xml`. Build: `android/app/build.gradle` · `android/build.gradle`. **Passkeys** [E10e]: `webauthn/PhlixWebAuthnModule.kt` (`ReactContextBaseJavaModule`, `getName()="PhlixWebAuthn"`, `@ReactMethod`-with-Promise isSupported/register/authenticate via AndroidX **Credential Manager** [`CredentialManager.create`/`getCredential` with `requestJson=optionsJson`] on `Dispatchers.Main` coroutines off `currentActivity`; Credential Manager CONSUMES/PRODUCES the standard WebAuthn JSON directly so the bridge is a thin **pass-through — NO manual base64url on Android**; cancel → `user_canceled`) + `webauthn/PhlixWebAuthnPackage.kt` (`ReactPackage`, registered in `MainApplication.kt` via `add(PhlixWebAuthnPackage())`). REQUIRES the two `androidx.credentials:credentials` + `:credentials-play-services-auth` `1.3.0` deps added to `android/app/build.gradle` [Credential Manager needs compileSdk 34+; RN 0.85 default is fine]. Runtime is DEVICE-ONLY — CI compiles only.

## Conventions

- **TS strict**: `tsconfig.json` has `strict: true`, alias `@/* → src/*`. Never use `any` for public API surface; screens may use `NativeStackNavigationProp<any>` for cross-stack nav.
- **Imports**: relative within `src/` (no `@/` alias usage in current code) — match existing files.
- **Components**: `React.FC<Props>` arrow functions, `StyleSheet.create({...})` at file bottom, **dark theme palette**: bg `#0f0f1a`, surface `#1a1a2e`, card `#2d2d44`, accent `#0066cc`, text `#fff`/`#888`. Match `android/app/src/main/res/values/colors.xml`.
- **Screens**: wrap in `<SafeContainer edges={['top']}>`, handle `isLoading`/`error` with `<LoadingSpinner fullScreen />` / `<ErrorView onRetry={...}>` / `<EmptyState>`.
- **Stores**: `create<State>((set, get) => ({...}))`, expose `setX` mutators, async actions wrap in try/catch and set `error`/`isLoading`. Settings stores auto-persist via `get().saveSettings()` after each setter.
- **API managers**: classes with async methods returning typed promises, exported as `export const xManager = new XManager(); export default xManager;`.
- **AsyncStorage keys**: prefixed `phlix_` (e.g. `phlix_downloads`, `phlix_settings`).
- **Time units**: ticks are 100ns (divide by `10000000` for seconds, by `600000000` for minutes — see `MediaDetailScreen.tsx`/`formatters.ts`).
- **Navigation params**: declared in `src/types/navigation.ts`; `Player` accepts `{ itemId: string; startPosition?: number; streamUrl?: string; liveTitle?: string }`. The `streamUrl`/`liveTitle` fields are **ADDITIVE (E8)**: when `streamUrl` is present the player plays it verbatim and SKIPS the itemId detail-fetch/transcode/markers lifecycle; absent → the existing itemId path is 100% unchanged.

## Testing

Jest preset `react-native` (`jest.config.js`). Setup in `jest.setup.js` mocks `react-native-safe-area-context`, `@react-navigation/native`, `react-native-gesture-handler`. Test layout: `src/<dir>/__tests__/<file>.test.ts`. `moduleNameMapper`: `^@/(.*)$` → `<rootDir>/src/$1`. Coverage from `src/**/*.{ts,tsx}` excluding `*.d.ts` and `index.ts`.

```bash
npm test                                       # run full jest suite
npm test -- --watch                            # interactive watch mode
npm test -- --testPathPattern="api"            # only api manager tests
npm test -- --testPathPattern="stores"         # only zustand store tests
npm test -- --coverage --collectCoverageFrom="src/**/*.{ts,tsx}"  # coverage report
npm test -- --runInBand                        # serial mode for debugging
npm test -- -u                                 # update snapshots
```

## CI/CD

`.github/workflows/test.yml` (push/PR), `.github/workflows/build-android.yml`, `.github/workflows/build-ios.yml` (manual macOS runner).

```bash
cd android && ./gradlew bundleRelease          # build AAB for Play Store upload
cd android && ./gradlew assembleRelease        # build signed APK
cd ios && xcodebuild -workspace PhlixMobile.xcworkspace -scheme PhlixMobile -configuration Release  # iOS release build
npm run lint -- --fix                          # auto-fix lint errors
npm run typecheck && npm run lint && npm test  # full pre-push gate
```

## Gotchas

- `BASE_URL` is hardcoded in `src/api/client.ts` — change there for self-hosted.
- iOS player is a **LocalPod** under `ios/LocalPods/PhlixPlayer/`; bump `podspec` and re-run `pod install` after Swift changes.
- Android Kotlin code references `MainActivity` for PiP — `PhlixPlayerView.kt` casts `reactApplicationContext.currentActivity as? MainActivity`.
- Push-notification typings live in `src/types/react-native-push-notification.d.ts`.
- `proguard-rules.pro` at `android/app/proguard-rules.pro`.
- **Passkeys (E10e) are DEVICE-ONLY at runtime** — the platform passkey ceremony shows a system biometric/PIN sheet that NO simulator/emulator (and no CI runner) can drive; CI compiles the iOS Swift / Android Kotlin only. The JS seam is fully Jest-tested (native mocked). Cross-device passkey portability also needs **relying-party host infra** the server publishes — iOS `apple-app-site-association` `webcredentials` + Android Digital Asset Links (`assetlinks.json`) for the `rpId` the server returns. That's server/host config, NOT mobile code (the native modules read `rpId` straight from the server options) — flag it for deploy.

<!-- caliber:managed:pre-commit -->
## Before Committing

**IMPORTANT:** Before every git commit, you MUST ensure Caliber syncs agent configs with the latest code changes.

First, check if the pre-commit hook is already installed:
```bash
grep -q "caliber" .git/hooks/pre-commit 2>/dev/null && echo "hook-active" || echo "no-hook"
```

- If **hook-active**: the hook handles sync automatically — just commit normally. Tell the user: "Caliber will sync your agent configs automatically via the pre-commit hook."
- If **no-hook**: run Caliber manually before committing:
  1. Tell the user: "Caliber: Syncing agent configs with your latest changes..."
  2. Run: `caliber refresh && git add CALIBER_LEARNINGS.md CLAUDE.md .claude/ .opencode/ 2>/dev/null`
  3. After it completes, briefly tell the user what Caliber updated. Then proceed with the commit.

**Valid `caliber refresh` options:** `--quiet` (suppress output) and `--dry-run` (preview without writing). Do not pass any other flags — options like `--auto-approve`, `--debug`, or `--force` do not exist and will cause errors.

**`caliber config`** takes no flags — it runs an interactive provider setup. Do not pass `--provider`, `--api-key`, or `--endpoint`.

If `caliber` is not found, tell the user: "This project uses Caliber for agent config sync. Run /setup-caliber to get set up."
<!-- /caliber:managed:pre-commit -->

<!-- caliber:managed:learnings -->
## Session Learnings

Read `CALIBER_LEARNINGS.md` for patterns and anti-patterns learned from previous sessions.
These are auto-extracted from real tool usage — treat them as project-specific rules.
<!-- /caliber:managed:learnings -->

<!-- caliber:managed:model-config -->
## Model Configuration

Recommended default: `claude-sonnet-4-6` with high effort (stronger reasoning; higher cost and latency than smaller models).
Smaller/faster models trade quality for speed and cost — pick what fits the task.
Pin your choice (`/model` in Claude Code, or `CALIBER_MODEL` when using Caliber with an API provider) so upstream default changes do not silently change behavior.

<!-- /caliber:managed:model-config -->

<!-- caliber:managed:sync -->
## Context Sync

This project uses [Caliber](https://github.com/caliber-ai-org/ai-setup) to keep AI agent configs in sync across Claude Code, Cursor, Copilot, and Codex.
Configs update automatically before each commit via `caliber refresh`.
If the pre-commit hook is not set up, run `/setup-caliber` to configure everything automatically.
<!-- /caliber:managed:sync -->
